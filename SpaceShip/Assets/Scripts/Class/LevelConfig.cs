namespace SpaceShip
{
    public class LevelConfig
    {
        public Level SelectedLevelSettings;

        public LevelConfig(Level selectedLevelSettings)
        {
            this.SelectedLevelSettings = selectedLevelSettings;
        }
    }
}